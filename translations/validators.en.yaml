production:
  customer:
    number:
      unique: This customer number is already in use.
  customer_project:
    number:
      unique: This project number is already in use.
    completion_requires_serials: A project can only be completed or delivered when every position has exactly one device or assembly with a valid serial number.
  build_instance:
    template_required: Select a system template or a Part-DB build project.
    notes_required_without_serial: A reason must be entered in the notes when no serial number is assigned.
    serial_required_for_completed_project: A serial number is required for devices in completed or delivered projects.
    project_position:
      unique: A device or assembly is already assigned to this project position.
    serial_number:
      unique: This serial number is already in use.
  project_position:
    template_required: Select a system template or a Part-DB build project.
    name:
      unique: A project position with this name already exists.
  system_template:
    base_project:
      unique: A system template already exists for this Part-DB project.
    slot:
      position:
        unique: This position number is already used in the system template.
      quantity:
        invalid: Maximum quantity must not be lower than minimum quantity.
      allowed_content:
        required: Select at least one allowed system template, Part-DB project or inventory part.
        cyclic: This selection would create a cyclic system-template hierarchy.
