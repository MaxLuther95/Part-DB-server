<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ revision.template.productTitle }}</title>
    <style>
        @page { size: A4 portrait; margin: 32mm 14mm 30mm; }
        body { margin: 0; }
        {% include 'production/datasheet/_styles.css.twig' %}
        .datasheet-watermark { position: fixed; }
        .datasheet-page-break span { display: none; }
        .datasheet-header { left: 0; right: 0; top: -23mm; position: fixed; margin: 0; height: 17mm; }
        .datasheet-header h1 { font-size: 16pt; }
        .datasheet-footer { bottom: -19mm; left: 0; position: fixed; right: 0; }
    </style>
</head>
<body class="datasheet-page">
    {% include 'production/datasheet/_document.html.twig' with {pdf_layout: true} %}
</body>
</html>
